Dokumen riset (PDF/teks) → Chunk → Embed → ChromaDB →
User input → retrieve top-k chunks → inject ke prompt llm.py
 
- Tambah PDF laporan keuangan IDX → `add_documents()`
- Tambah berita saham terkini → bisa dari Yahoo Finance news API 
